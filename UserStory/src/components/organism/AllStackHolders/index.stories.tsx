import { Meta, StoryObj } from '@storybook/react'
import AllStackHolder from '.'

const meta: Meta<typeof AllStackHolder> = {
  component: AllStackHolder,
  tags: ['autodocs'],
}

type Story = StoryObj<typeof AllStackHolder>

export const Render: Story = {}

export default meta
